import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.application.clida',
  appName: 'CliDa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
